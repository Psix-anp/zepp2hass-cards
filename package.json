{
  "name": "zepp2hass-cards",
  "version": "1.0.3",
  "private": true,
  "type": "module",
  "description": "Community Home Assistant dashboard cards for Zepp2Hass",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check zepp2hass-cards.js && npm test && node scripts/privacy-scan.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
